<Window x:Class="AudioVideoLib.Studio.Editors.Id3v2.MlltEditorDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Width="640" Height="480" WindowStartupLocation="CenterOwner"
        Title="MPEG location lookup table (MLLT)">
    <DockPanel Margin="14">
        <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,14,0,0">
            <Button Content="OK" Width="80" Click="Ok_Click" IsDefault="True" />
            <Button Content="Cancel" Width="80" Margin="8,0,0,0" Click="Cancel_Click" IsCancel="True" />
        </StackPanel>
        <StackPanel DockPanel.Dock="Top" Margin="0,0,0,8">
            <TextBlock Text="MPEG frames between reference"
                       Foreground="{DynamicResource TextSecondaryBrush}" FontSize="11" />
            <TextBox Text="{Binding MpegFramesBetweenReference, UpdateSourceTrigger=PropertyChanged}"
                     Padding="4,3" Margin="0,0,0,8" />
            <TextBlock Text="Bytes between reference"
                       Foreground="{DynamicResource TextSecondaryBrush}" FontSize="11" />
            <TextBox Text="{Binding BytesBetweenReference, UpdateSourceTrigger=PropertyChanged}"
                     Padding="4,3" Margin="0,0,0,8" />
            <TextBlock Text="Milliseconds between reference"
                       Foreground="{DynamicResource TextSecondaryBrush}" FontSize="11" />
            <TextBox Text="{Binding MillisecondsBetweenReference, UpdateSourceTrigger=PropertyChanged}"
                     Padding="4,3" Margin="0,0,0,8" />
        </StackPanel>
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,6">
            <Button Content="Add"    Width="70"                  Click="Add_Click" />
            <Button Content="Remove" Width="70" Margin="6,0,0,0" Click="Remove_Click" />
            <Button Content="Up"     Width="70" Margin="6,0,0,0" Click="Up_Click" />
            <Button Content="Down"   Width="70" Margin="6,0,0,0" Click="Down_Click" />
        </StackPanel>
        <DataGrid x:Name="Grid" ItemsSource="{Binding Entries}"
                  AutoGenerateColumns="False" CanUserAddRows="False" CanUserDeleteRows="False"
                  HeadersVisibility="Column" SelectionMode="Single" SelectionUnit="FullRow">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Deviation (bytes)" Binding="{Binding DeviationInBytes}" Width="*" />
                <DataGridTextColumn Header="Deviation (ms)"    Binding="{Binding DeviationInMilliseconds}" Width="*" />
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
</Window>
