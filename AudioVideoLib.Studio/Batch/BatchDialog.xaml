<Window x:Class="AudioVideoLib.Studio.Batch.BatchDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Batch scan folder"
        Width="760"
        Height="520"
        WindowStartupLocation="CenterOwner">
    <DockPanel Margin="12">
        <DockPanel DockPanel.Dock="Top" Margin="0,0,0,8">
            <Button DockPanel.Dock="Right" Content="Pick folder..." Padding="10,4" Margin="6,0,0,0" Click="Pick_Click" />
            <TextBox x:Name="FolderBox" VerticalContentAlignment="Center" Padding="4" />
        </DockPanel>
        <DockPanel DockPanel.Dock="Top" Margin="0,0,0,8">
            <CheckBox x:Name="RecursiveBox" Content="Recursive" IsChecked="True" VerticalAlignment="Center" Margin="0,0,12,0" />
            <Button DockPanel.Dock="Right" Content="Save report as..." Padding="10,4" Margin="8,0,0,0" Click="Save_Click" />
            <Button DockPanel.Dock="Right" Content="Scan" Padding="10,4" Click="Scan_Click" />
            <TextBlock x:Name="SummaryText" Margin="12,0,0,0" VerticalAlignment="Center"
                       Foreground="{DynamicResource TextSecondaryBrush}" Text="Ready." />
        </DockPanel>
        <DataGrid x:Name="Grid"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  IsReadOnly="True"
                  HeadersVisibility="Column"
                  GridLinesVisibility="Horizontal"
                  FontFamily="Consolas"
                  FontSize="11"
                  RowHeight="20">
            <DataGrid.Columns>
                <DataGridTextColumn Header="File" Binding="{Binding Path}" Width="*" />
                <DataGridTextColumn Header="Size" Binding="{Binding Size, StringFormat=N0}" Width="90" />
                <DataGridTextColumn Header="Format" Binding="{Binding Format}" Width="130" />
                <DataGridTextColumn Header="Tags" Binding="{Binding Tags}" Width="120" />
                <DataGridTextColumn Header="Err" Binding="{Binding Errors}" Width="40" />
                <DataGridTextColumn Header="Warn" Binding="{Binding Warnings}" Width="45" />
                <DataGridTextColumn Header="Error" Binding="{Binding Error}" Width="200" />
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
</Window>
