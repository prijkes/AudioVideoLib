<Window x:Class="AudioVideoLib.Studio.EditApeItemDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Edit APE item"
        Width="520"
        Height="420"
        MinWidth="420"
        MinHeight="300"
        ResizeMode="CanResize"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False">
    <DockPanel Margin="14">
        <DockPanel.Resources>
            <Style x:Key="LabelStyle" TargetType="TextBlock">
                <Setter Property="Foreground" Value="#666666" />
                <Setter Property="FontSize" Value="11" />
                <Setter Property="Margin" Value="0,0,0,2" />
            </Style>
        </DockPanel.Resources>

        <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,14,0,0">
            <Button Content="OK"     Width="80"                  Click="OkButton_Click"     IsDefault="True" />
            <Button Content="Cancel" Width="80" Margin="8,0,0,0" Click="CancelButton_Click" IsCancel="True" />
        </StackPanel>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Style="{StaticResource LabelStyle}" Text="Key" />
            <TextBox   Grid.Row="1" x:Name="KeyBox" IsReadOnly="True" Padding="4,3" Margin="0,0,0,8" />

            <TextBlock Grid.Row="2" x:Name="ValueLabel" Style="{StaticResource LabelStyle}" Text="Values (one per line)" />
            <TextBox   Grid.Row="3" x:Name="ValueBox" Padding="4,3"
                       AcceptsReturn="True" TextWrapping="Wrap"
                       VerticalScrollBarVisibility="Auto"
                       FontFamily="Consolas, Courier New, monospace" />

            <TextBlock Grid.Row="4" x:Name="HintText" Style="{StaticResource LabelStyle}"
                       Margin="0,6,0,0" TextWrapping="Wrap"
                       Text="Each non-empty line is stored as a separate value (APEv2 separates multi-values internally with a NUL byte)." />
        </Grid>
    </DockPanel>
</Window>
